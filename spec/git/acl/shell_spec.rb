require "spec_helper"

describe Git::Acl::Shell do
  it "has a version number" do
    expect(Git::Acl::Shell::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
